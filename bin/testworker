#!/usr/bin/perl

use strict;
use warnings;

use JSON::XS;
use Smart::Worker;

my $conn = Smart::Worker->connection;
my $body = JSON::XS::encode_json({ hostname => '127.0.0.1', filename => 'worker.js' });
$conn->send({ destination => '/queue/smart/work', body => $body });
